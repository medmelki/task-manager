package com.taskmanager.service;

import com.taskmanager.model.Picture;
import org.springframework.stereotype.Service;

@Service
public interface IPictureService extends IGenericService<Picture, Integer> {
}
